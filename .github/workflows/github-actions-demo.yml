name: learn-github-actions
on: [push]
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: ls /
  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - run: ls /
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: ls /
